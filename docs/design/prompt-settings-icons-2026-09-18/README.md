# Boards approved by Aaron on 2026-09-18 ("I love it. Build it.")

boards.html draws its glyphs from a `fluent-icons.js` extracted from @fluentui/react-icons (6 MB, not committed). Regenerate it with:

    node -e "..."  # see the session notes; or read the PNGs, which are the approved renders.
