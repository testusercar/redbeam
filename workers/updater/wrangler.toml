# REDBEAM in-place update channel.
#
# Deploy from this directory:
#   npx wrangler deploy
#
# The bucket name is the R2 bucket you create in the Cloudflare dashboard
# (or `npx wrangler r2 bucket create redbeam-updates`). No account id and no
# API token belong in this file — wrangler reads those from the login.
name = "redbeam-updates"
main = "src/index.ts"
compatibility_date = "2026-09-23"

[[r2_buckets]]
binding = "UPDATES"
bucket_name = "redbeam-updates"
